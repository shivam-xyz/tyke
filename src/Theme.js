import { createTheme } from "@mui/material";

const Theme = createTheme({
    palette:{
        primary:{
            main: '#000000',
            secondary:'#3f51b5'
        }
    }
})

export default Theme;